.venv/
__pycache__/
*.pyc
.claude/
.DS_Store

# local-only source config (endpoints not committed)
src/local_config.py

# raw source snapshots are local-only; keep only the placeholder README
data/raw/*
!data/raw/README.md
